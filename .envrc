watch_file flake.nix
watch_file dev.sh
watch_file nix
if [ -d nix ]; then
  while IFS= read -r nix_module; do
    watch_file "$nix_module"
  done < <(find nix -type f -name '*.nix' -print | sort)
fi

use flake
